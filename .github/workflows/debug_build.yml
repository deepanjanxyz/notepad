name: Debug APK

on:
  push:
    paths-ignore:
      # Documentation
      - '**/*.md'
      - '**/*.txt'
      - '**/*.rst'
      - '**/*.adoc'

      # Images
      - '**/*.png'
      - '**/*.jpg'
      - '**/*.jpeg'
      - '**/*.gif'
      - '**/*.webp'
      - '**/*.bmp'
      - '**/*.tif'
      - '**/*.tiff'
      - '**/*.ico'
      - '**/*.avif'

      # Design / vector assets
      - '**/*.svg'
      - '**/*.psd'
      - '**/*.ai'
      - '**/*.sketch'

      # Video / audio assets
      - '**/*.mp4'
      - '**/*.mkv'
      - '**/*.webm'
      - '**/*.mov'
      - '**/*.avi'
      - '**/*.mp3'
      - '**/*.wav'
      - '**/*.ogg'
      - '**/*.flac'

      # Repository metadata / miscellaneous non-build files
      - '**/.editorconfig'
      - '**/.gitattributes'
      - '**/.gitignore'
      - '**/.mailmap'

  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: debug-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-debug:
    name: Build Debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up OpenJDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      # app/build.gradle.kts validates release signing at configuration time for
      # every build type (including debug): it requires a regular keystore at
      # app/keystore.jks plus the NOTEPAD_* values. The debug APK itself is still
      # signed with the standard Android debug key; this keystore only satisfies
      # that validation gate.
      - name: Create keystore required by the signing validation
        run: |
          keytool -genkeypair \
            -keystore app/keystore.jks \
            -storepass android \
            -alias androiddebugkey \
            -keypass android \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -dname "CN=Android Debug,O=Android,C=US"

      - name: Build debug APK
        env:
          NOTEPAD_STORE_PASSWORD: android
          NOTEPAD_KEY_ALIAS: androiddebugkey
          NOTEPAD_KEY_PASSWORD: android
        run: ./gradlew assembleDebug --no-daemon

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: '**/build/outputs/apk/debug/*.apk'
          if-no-files-found: error
          retention-days: 14
